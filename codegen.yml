overwrite: true
schema: "https://anime-app-plus.hasura.app/v1/graphql"
documents: "./src/graphql/**/*.ts"
generates:
  ./src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      fetcher: graphql-request
      exposeQueryKeys: true
