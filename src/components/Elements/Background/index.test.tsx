import { render } from "@testing-library/react";
import { Background } from "src/components/Elements/Background";

describe("Components/Elements/Background", () => {
  test("should render", () => {
    render(<Background />);
  });
});
